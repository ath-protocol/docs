---
title: "Gateway Architecture"
description: "Internal architecture of the ATH reference gateway."
---

## Component Architecture

```mermaid
graph TB
    subgraph "HTTP Layer - Hono"
        Routes["Routes<br/>/ath/*, /.well-known/*, /auth/*, /ui/*"]
        Middleware["Middleware<br/>CORS, Auth, Admin"]
    end
    
    subgraph "Protocol Layer"
        AttestVerify["Attestation Verifier<br/>+ JTI Replay Cache"]
        ScopeEngine["Scope Intersection"]
        OAuthBridge["OAuth Bridge<br/>openid-client / mock"]
    end
    
    subgraph "Storage Layer - In-Memory"
        UserStore["User Store"]
        AgentStore["Agent Registry"]
        SessionStore["Session Store"]
        TokenStore["Token Store"]
        ProviderTokenStore["Provider Tokens"]
        ProviderConfig["Provider Config"]
    end
    
    Routes --> Middleware
    Middleware --> AttestVerify
    Middleware --> ScopeEngine
    Middleware --> OAuthBridge
    
    AttestVerify --> AgentStore
    ScopeEngine --> TokenStore
    OAuthBridge --> ProviderTokenStore
    OAuthBridge --> ProviderConfig
```

## OAuth Modes

```mermaid
graph TD
    Start["Gateway Start"] --> Check{"Providers configured?"}
    Check -->|"Yes"| Direct["Direct Mode<br/>Real OAuth via openid-client"]
    Check -->|"No"| Mock["Mock Mode<br/>Simulated consent flow"]
    
    Direct --> PKCE["PKCE S256 authorization"]
    Direct --> Exchange["Real token exchange"]
    Direct --> RealProxy["Real upstream proxy"]
    
    Mock --> FakeURL["Mock consent URL"]
    Mock --> FakeToken["Mock token response"]
    Mock --> MockProxy["Mock proxy response"]
```

| Feature | Direct Mode | Mock Mode |
|---------|------------|-----------|
| **OAuth URLs** | Real provider endpoints | `/ui/mock-consent` |
| **PKCE** | Real S256 | Simulated |
| **Provider tokens** | Real upstream tokens | Placeholder |
| **Proxy** | Forwards to real APIs | Mock data |
| **Use case** | Production | Development |

## Storage Details

All stores are **in-memory** in the reference implementation. For production, replace with persistent backends:

| Store | What to Replace With |
|-------|---------------------|
| Agent Registry | PostgreSQL / Redis |
| Session Store | Redis with TTL |
| Token Store | PostgreSQL with expiry index |
| Provider Token Store | Encrypted database |
| User Store | PostgreSQL with bcrypt/scrypt |

<Card title="Next: Production Deployment" icon="arrow-right" href="/gateway/production">
  Considerations for deploying the gateway in production
</Card>

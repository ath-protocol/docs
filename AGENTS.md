# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently empty (only a blank `README.md`). There are no services, dependencies, build steps, or tests to run.

When code is added to this repository, update this section with:
- How to install dependencies and which package manager to use
- How to run lint, test, and build commands
- How to start development servers or services
- Any non-obvious caveats discovered during setup
